rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules for thisAItech HR app.
    // Tighten these rules before production and move auth to Firebase Authentication.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
